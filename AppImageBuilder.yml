version: 1
AppDir:
  path: ./AppDir
  app_info:
    id: org.icesculpt.IceSculpt
    name: IceSculpt
    icon: icesculpt
    version: 0.1.0
    exec: usr/bin/icesculpt
    exec_args: "$@"
  apt:
    arch:
      - amd64
    sources:
      - sourceline: deb [arch=amd64] http://archive.ubuntu.com/ubuntu/ jammy main restricted universe multiverse
        key_url: http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x871920D1991BC93C
      - sourceline: deb [arch=amd64] http://archive.ubuntu.com/ubuntu/ jammy-updates main restricted universe multiverse
        key_url: http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x871920D1991BC93C
      - sourceline: deb [arch=amd64] http://security.ubuntu.com/ubuntu/ jammy-security main restricted universe multiverse
        key_url: http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x871920D1991BC93C
    include:
      - python3
      - python3-gi
      - python3-gi-cairo
      - gir1.2-gtk-3.0
      - libgtk-3-0
      - libgirepository-1.0-1
      - libcairo2
      - libdbus-1-3
      - libcanberra-gtk3-module
      - libglib2.0-0
      - libgdk-pixbuf2.0-0
      - shared-mime-info
      - desktop-file-utils
      - adwaita-icon-theme
      - hicolor-icon-theme
  runtime:
    env:
      PYTHONHOME: '${APPDIR}/usr'
      PYTHONPATH: '${APPDIR}/usr/lib/python3.10/site-packages'

AppImage:
  arch: x86_64
  update-information: None
  sign-key: None

script:
  - export DESTDIR=$TARGET_APPDIR
  - apt-get update || true
  - apt-get install -y gnupg
  - gpg --keyserver hkps://keyserver.ubuntu.com --recv-keys 871920D1991BC93C 3B4FE6ACC0B21F32 || gpg --keyserver hkp://keyserver.ubuntu.com --recv-keys 871920D1991BC93C 3B4FE6ACC0B21F32 || true
  - gpg --export 871920D1991BC93C | apt-key add - || true
  - gpg --export 3B4FE6ACC0B21F32 | apt-key add - || true
  - mkdir -p $DESTDIR/usr/lib/python3.10/site-packages
  - cp -r icesculpt $DESTDIR/usr/lib/python3.10/site-packages/
  - mkdir -p $DESTDIR/usr/bin
  - cp icesculpt/icesculpt-launcher $DESTDIR/usr/bin/icesculpt
  - chmod +x $DESTDIR/usr/bin/icesculpt
  - mkdir -p $DESTDIR/usr/share/icons/hicolor/scalable/apps
  - cp icesculpt/data/icons/icesculpt.svg $DESTDIR/usr/share/icons/hicolor/scalable/apps/icesculpt.svg
  - mkdir -p $DESTDIR/usr/share/applications
  - echo -e "[Desktop Entry]\nType=Application\nName=IceSculpt\nExec=icesculpt\nIcon=icesculpt\nCategories=Utility;Settings;" > $DESTDIR/usr/share/applications/org.icesculpt.IceSculpt.desktop
  - update-mime-database $DESTDIR/usr/share/mime || true
